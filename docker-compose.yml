services:
  dexter-write:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dexter-write
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
